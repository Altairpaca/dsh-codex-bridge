# Sensitive / local-only files that must never be committed.
# Credentials and tokens live outside this repo in ~/.dsh and ~/.codex.
.credentials.yaml
auth.json
*.env
.env*
*.local
.dsh/
*.snapshot.json
snapshot.json
*.lock

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
.idea/
.vscode/
